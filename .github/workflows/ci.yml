name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "pentest fixture - disabled"
